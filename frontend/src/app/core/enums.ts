export enum UserRole  {
  admin = "admin",
  user = "user"
};


export enum Status  {
  active = 'active',
  deleted = 'deleted'
};


export enum Theme {
  system = 'system',
  dark = "dark",
  light = "light"
}

export enum Language {
  system = "system",
  enUs = 'en-us',

}